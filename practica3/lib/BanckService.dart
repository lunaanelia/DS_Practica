import 'package:practica3/User.dart';

class BankService {
  Set<User> _users = {};

  BankService () {}

  // Create account le crea la cuenta al usuario que se le indique
  // Si el usuario no existe, lo crea y lo añade al Set de usuarios
  // Si no existe, lo crea
  createAccount (String id, String name) {
    //this._users.lookup(object)
  }

}