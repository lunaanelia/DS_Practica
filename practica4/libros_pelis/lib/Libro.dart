import 'package:libros_pelis/Producto.dart';

class Libro extends Producto {

  Libro(String titulo, String autor, DateTime fecha, String descripcion)
      :super(titulo, autor, fecha, descripcion);

}